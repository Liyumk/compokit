import { ReactNode } from "react";
export interface GlobalStyleWrapperProps {
    children: ReactNode;
}
declare const GlobalStyleWrapper: (props: GlobalStyleWrapperProps) => JSX.Element;
export default GlobalStyleWrapper;
